#include <iostream>

using namespace std;

int main() {
    int a, b, c, s;         // int定义a, b, c, s为整形变量
    cin >> a >> b >> c;     // 输入给定3个整数的值
    s = a + b + c;          // 求3个整数的和
    cout << s << endl;      // 输出s的值
    return 0;
}
